# Git Workflow

For every milestone:

```text
Implement
→ Test
→ Review
→ Update docs
→ Commit
→ Push
→ Next milestone
```

Suggested migration commit:

```text
feat(database): harden migration runner
```
